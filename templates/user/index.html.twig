{% extends 'base.html.twig' %}

{% block title %}Profil de {{ app.user.pseudo }}{% endblock %}

{% block body %}

    <div class="container-fluid container-user justify-content-center">
        <div class="row d-flex justify-content-between">
            <div class="col-4">
                <h1>Paramètre du compte</h1>
            </div>
            <div class="col-4 d-flex justify-content-center">
                <a href="{{ path('app_logout') }}"><button class="btn button-decoration"><i class="fas fa-power-off"></i> Déconnexion</button></a>
            </div>
        </div>

        <div class="row d-flex flex-column justify-content-center text-center">
            <div class="col-12">
                <h2>Bonjour : {{ app.user.pseudo }} !</h2>
            </div>
            <div class="col-12">
                <h3>{{ app.user.email }}</h3>
            </div>

        </div>

        {% include 'component/_card_user.html.twig' %}


    </div>



{% endblock %}

